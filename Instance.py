from Stream import Stream


movie = Stream(input("Enter a movie to search for:\n"))

movie.print_info()


# append matches of movie ID's and their titles to a dictionary / use a number to associate a movie with a title / id, 1 = id, 2 = next id
# User inputs which title they are interested in 
# get cast / crew / other methods or just output that movies streaming services as well as availability to rent / buy 